events {
  worker_connections 1024;
}
http {
  server {
    listen 8080;
    server_name _;
    location / {
      return 200 'My proxy is working!';
      add_header Content-Type text/plain;
    }
  }
}
